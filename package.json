{
  "name": "flix-iptv",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2026-03-06",
    "build": "echo 'No build step needed'",
    "deploy": "wrangler pages deploy . --project-name=flix-iptv"
  },
  "dependencies": {
    "hono": "^4.12.5"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}